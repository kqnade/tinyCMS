{
  "name": "@tinycms/public-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir ../../dist/public-worker",
    "dev": "wrangler dev --ip 127.0.0.1 --port 8787 --inspector-port 9229",
    "test": "vitest run",
    "typecheck": "wrangler types --include-runtime false --check && tsc -p tsconfig.json"
  },
  "dependencies": {
    "@tinycms/contracts": "workspace:*",
    "hono": "4.13.2"
  }
}
